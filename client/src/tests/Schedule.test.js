import React from 'react'
import Schedule from '../components/Schedule'
import renderer from 'react-test-renderer'

it('exists', () => {

})

it('renders a div named schedule', () => {
  console.log(Schedule())
})
